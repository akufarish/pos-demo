<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="20dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:layout_marginTop="@dimen/spacing_transaksi_item"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:id="@+id/namaBarang"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/gray"
        android:text="@string/bayaran_label"
        android:textSize="18sp" />

    <TextView
        android:layout_marginTop="@dimen/spacing_transaksi_item"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/black"
        android:text="10000"
        app:layout_constraintStart_toEndOf="@+id/namaBarang"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginStart="12dp"
        android:id="@+id/pcs"
        android:textSize="15sp"
        android:textStyle="bold"/>

    <TextView
        android:layout_marginTop="@dimen/spacing_transaksi_item"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/black"
        android:text="10000"
        app:layout_constraintStart_toEndOf="@+id/pcs"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginStart="12dp"
        android:id="@+id/hargaSatuan"
        android:textSize="15sp"
        android:textStyle="bold"/>

    <TextView
        android:layout_marginTop="@dimen/spacing_transaksi_item"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/black"
        android:text="10000"
        app:layout_constraintStart_toEndOf="@+id/hargaSatuan"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginStart="12dp"
        android:id="@+id/total"
        android:textSize="15sp"
        android:textStyle="bold"/>

</androidx.constraintlayout.widget.ConstraintLayout>